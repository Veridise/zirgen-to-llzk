// RUN: zklang -o - %s --print-debug-info=false | FileCheck %s

component Unpack<N: Val, P: Val>(in: Array<Val, N/P>) {
  in[0]
}

// CHECK-LABEL: func @compute() -> !llzk.struct<@User1<[]>> {
component User1() {
  Unpack<8,2>([0,0,0,0])
// CHECK: %{{[0-9a-zA-Z_\.]+}} = call @Unpack::@compute(%{{[0-9a-zA-Z_\.]+}}) : (!llzk.array<4 x !llzk.felt>) -> !llzk.struct<@Unpack<[8, 2, 4]>> 
// CHECK-LABEL: func @constrain(%{{[0-9a-zA-Z_\.]+}}: !llzk.struct<@User1<[]>>) {
}
